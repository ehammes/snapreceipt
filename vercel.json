{
  "version": 2,
  "buildCommand": "cp -r client/api . && cd client && npm install && npm run build",
  "outputDirectory": "client/build",
  "framework": null,
  "installCommand": "npm install --prefix client"
}
